to execute our project and replicate our results for scalability,
make the script executable with `chmod +x series_1_runner.sh`, then simply run it.

if you want to run our project from the VS code extension, remove the `list[str] args`
argument from the main function.
